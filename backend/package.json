{
  "name": "language-practice-partner-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Backend for Language Practice Partner: owns the OpenAI Realtime voice session and the coaching domain (languages, difficulty levels, scenarios, correction + scoring + goal tools).",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "ws": "^8.18.0"
  }
}
