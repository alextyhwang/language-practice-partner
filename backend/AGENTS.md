# ⛔ LOCKED — AI agents must not modify this directory

This entire `backend/` directory is **off-limits to AI agents**.

Do **not** create, edit, move, rename, or delete any file here. Do not refactor,
reformat, change dependencies, or run/restart the service. This applies to
Cursor, Claude, Codex, Copilot, and any other automated tool.

If a task appears to need a change in `backend/`, **stop and ask a human
maintainer first**. Only proceed when a human gives an explicit, direct
instruction that names this directory and authorizes the specific change.

See the repository root `AGENTS.md` for the full policy.
